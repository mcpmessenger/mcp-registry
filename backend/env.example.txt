# Environment template for SlashMCP.com backend
# Copy to `.env` and fill with your own secrets.

# Database
DATABASE_URL="postgresql://mcp_registry:your_secure_password@localhost:5432/mcp_registry"

# Server
PORT=3001
NODE_ENV=development
CORS_ORIGIN="http://localhost:3000"

# Google APIs
GOOGLE_GEMINI_API_KEY=your_gemini_key
GOOGLE_VISION_API_KEY=your_vision_key

# OpenAI (required for Whisper voice transcription)
OPENAI_API_KEY=your_openai_key

# Kafka (used for design pipeline)
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=mcp-registry-api-gateway
KAFKA_GROUP_ID=mcp-registry-workers
KAFKA_TOPIC_DESIGN_REQUESTS=design-requests
KAFKA_TOPIC_DESIGN_READY=design-ready

# Kafka (used for orchestrator pipeline)
ENABLE_KAFKA=true
KAFKA_TOPIC_USER_REQUESTS=user-requests
KAFKA_TOPIC_TOOL_SIGNALS=tool-signals
KAFKA_TOPIC_TOOL_SIGNALS_RETRY_5S=tool-signals-retry-5s
KAFKA_TOPIC_TOOL_SIGNALS_RETRY_30S=tool-signals-retry-30s
KAFKA_TOPIC_TOOL_SIGNALS_DLQ=tool-signals-dlq
KAFKA_TOPIC_ORCHESTRATOR_PLANS=orchestrator-plans
KAFKA_TOPIC_ORCHESTRATOR_RESULTS=orchestrator-results

# LangChain MCP (optional storefront)
LANGCHAIN_ENDPOINT=https://langchain-agent-mcp-server-554655392699.us-central1.run.app
LANGCHAIN_API_KEY=optional_langchain_key

# Exa MCP API key (optional)
# If you have an API key for Exa (https://exa.ai), set it here. When provisioning stock servers
# via Cloud Run jobs, prefer using Secret Manager and set as a secret: EXA_API_KEY=exa-api-key:latest
EXA_API_KEY=your_exa_api_key

# Encryption keys (keep them random and secret)
ENCRYPTION_SECRET=your_encryption_secret
ENCRYPTION_SALT=your_encryption_salt







